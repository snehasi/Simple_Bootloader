# Simple_Bootloader
A simple bootloader, which would be booted via UEFI (or legacy) BIOS. The bootloader would reside in the USB pen-drive. BIOS boots up the bootloader residing in the pen- drive, which would thereby switch the CPU to protected mode (64-bit mode).
